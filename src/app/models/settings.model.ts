export interface Settings {
    currentMode: number;
    currentPalette: string;
    currentEffect: string;
    currentColor: string;
    currentStep: number;
    brightness: number;
    fps: number;
    hasBlend: boolean;
}